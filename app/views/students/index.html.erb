<div class="row">
  <div class="col-md-12">
    <div class="content-box-large">
      <% if notice %>
      <div class="alert alert-success" role="alert">
        <%= notice %>
      </div>
      <% end %>
      <div class="col-md-12">
      <div class="col-md-4">
        <h3><%= t('list') + " "+ t('students') %></h3>
      </div>

      <div class="col-md-2 pull-left">
        <a href="<%= new_student_path %>" class="btn btn-primary"><i class="glyphicon glyphicon-plus"></i></a>
      </div>
    </div>
      <div class="panel-body">
        <table class="table table-bordered">
              <thead>
                <tr>
                  <th><%= t('name') %></th>
                  <th><%= t('image') %></th>
                  <th></th>
                </tr>
              </thead>
              <tbody>
                <% @students.each do |student| %>
                  <tr>
                    <td><%= student.name %></td>
                    <td><%= image_tag student.image_url, width: 100, height: 100 %></td>
                    <td>
                      <%= link_to student, class: 'btn btn-sm btn-default' do %>
                        <%= fa_icon 'eye' %>
                      <% end %>
                      <%= link_to edit_student_path(student), class: 'btn btn-sm btn-success' do %>
                        <%= fa_icon 'edit' %>
                      <% end %>
                      <%= link_to student, method: :delete, data: { confirm: t('are_you_sure') }, class: 'btn btn-sm btn-danger' do %>
                        <%= fa_icon 'trash-o' %>
                      <% end %>
                    </td>

                  </tr>
                <% end %>

              </tbody>
            </table>
      </div>
    </div>
  </div>
</div>
